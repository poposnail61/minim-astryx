# Copyright (c) Meta Platforms, Inc. and affiliates.

name: Compact GitHub Pages History

on:
  schedule:
    - cron: '17 9 * * 1' # Mondays at 09:17 UTC
  workflow_dispatch:

permissions: {}

concurrency:
  group: 'pages-deploy'
  cancel-in-progress: false

jobs:
  compact:
    if: github.repository == 'facebook/astryx'
    runs-on: ubuntu-slim
    permissions:
      actions: read
      contents: write
    steps:
      - name: Checkout maintenance scripts
        uses: actions/checkout@v7
        with:
          ref: main

      - name: Compact gh-pages history
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node .github/scripts/gh-pages-publisher.mjs compact
